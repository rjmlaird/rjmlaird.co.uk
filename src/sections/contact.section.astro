<section id="contact">
  <div class="container pg-shell">
    <p class="sl">Contact</p>

    <div class="contact-card">
      <div class="contact-card__content">
        <h2>Let’s build something meaningful.</h2>
        <p>
          Open to strategy, ESG comms, writing, speaking, and collaboration across
          the space sector, sustainability, and digital.
        </p>
      </div>

      <div class="contact-card__actions">
        <a class="contact-form" href="/contact">
          Use the contact form
        </a>

        <a
          class="contact-book"
          href="https://cal.com/rjmlaird/discovery"
          target="_blank"
          rel="noopener noreferrer"
        >
          Book a call
        </a>

        <p class="contact-note">
          Best for project inquiries, partnerships, editorial work, and speaking requests.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
#contact {
  padding-block: 5rem;
  background: var(--ink2);
}

.contact-card {
  position: relative;
  display: grid;
  gap: 1.5rem;
  padding: 1.5rem;
  border: 1px solid var(--bd);
  border-radius: 1rem;
  background:
    linear-gradient(180deg, rgba(255, 255, 255, 0.03), rgba(255, 255, 255, 0)),
    var(--card);
  overflow: hidden;
}

.contact-card::before {
  content: "";
  position: absolute;
  inset: 0 auto 0 0;
  width: 4px;
  background: var(--teal);
}

.contact-card__content h2 {
  margin: 0 0 0.75rem;
  font-family: var(--D);
  font-size: clamp(1.8rem, 4vw, 3rem);
  line-height: 1.05;
  letter-spacing: -0.03em;
}

.contact-card__content p {
  margin: 0;
  max-width: 52rem;
  color: var(--mb);
  line-height: 1.8;
}

.contact-card__actions {
  display: grid;
  gap: 0.85rem;
  max-width: 30rem;
}

.contact-form,
.contact-book {
  display: inline-flex;
  width: fit-content;
  align-items: center;
  justify-content: center;
  padding: 0.8rem 1.1rem;
  border-radius: 0.7rem;
  text-decoration: none;
  font-family: var(--D);
  font-size: 0.82rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  min-height: 44px;
}

.contact-form {
  background: var(--teal);
  color: var(--ink);
}

.contact-form:hover,
.contact-form:focus-visible {
  opacity: 0.88;
}

.contact-book {
  background: transparent;
  color: var(--w);
  border: 1px solid var(--bm);
}

.contact-book:hover,
.contact-book:focus-visible {
  border-color: var(--teal);
  color: var(--teal);
}

.contact-note {
  margin: 0;
  color: var(--mb);
  line-height: 1.7;
}

@media (min-width: 860px) {
  .contact-card {
    grid-template-columns: minmax(0, 1.3fr) minmax(320px, 0.7fr);
    align-items: start;
  }
}
</style>