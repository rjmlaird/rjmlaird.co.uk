<section id="hero" class="hero">
  <div class="hero__content">
    <p class="kk">Chartered Marketer · MPhys · Space · Sustainability</p>
    <h1 class="nm">Ryan<br /><span class="d">Laird</span></h1>
    <p class="creds">MPhys (Hons) · CMktr · MInstP · MCIPR</p>
    <p class="hp">Turning science into story for the space sector.</p>
    <div class="hb">
      <a href="#story" class="bp">Read story</a>
      <a href="#work" class="bo">Projects</a>
    </div>
  </div>

  <div class="hero__visual">
    <div class="hero__portrait">
      <img
        src="../assets/img/person/ryan-laird.jpg"
        alt="Ryan Laird portrait"
        width="720"
        height="900"
        loading="eager"
        fetchpriority="high"
        decoding="async"
      />
    </div>
  </div>
</section>

<style>
#hero {
  position: relative;
  min-height: 100vh;
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(320px, 420px);
  grid-template-areas: "content visual";
  align-items: end;
  justify-content: space-between;
  width: 100%;
  padding: 0 var(--gutter) 5rem;
  overflow: hidden;
}

#hero .hero__content {
  grid-area: content;
  justify-self: start;
  align-self: end;
  text-align: left;
  width: 100%;
  max-width: 44rem;
  display: grid;
  gap: 0.25rem;
}

#hero .hero__visual {
  grid-area: visual;
  justify-self: end;
  align-self: end;
  width: 100%;
  display: flex;
  justify-content: flex-end;
}

#hero .hero__portrait {
  position: relative;
  width: min(100%, 420px);
  aspect-ratio: 4 / 5;
  border-radius: 1.25rem;
  overflow: hidden;
  border: 1px solid var(--bd);
  background: var(--card);
  margin-left: auto;
}

#hero .hero__portrait img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center top;
  display: block;
}

#hero .orb {
  position: absolute;
  right: -50px;
  top: 50%;
  transform: translateY(-52%);
  width: 520px;
  height: 520px;
  pointer-events: none;
  opacity: 0.09;
}

#hero .rA {
  transform-origin: 260px 260px;
  animation: cw 65s linear infinite;
}

#hero .rB {
  transform-origin: 260px 260px;
  animation: ccw 42s linear infinite;
}

#hero .rC {
  transform-origin: 260px 260px;
  animation: cw 24s linear infinite;
}

#hero .kk {
  font-family: var(--D);
  font-size: 0.7rem;
  font-weight: 700;
  letter-spacing: 0.22em;
  text-transform: uppercase;
  color: var(--teal);
  margin: 0 0 1.2rem;
  display: flex;
  align-items: center;
  gap: 0.7rem;
}

#hero .kk::before {
  content: "";
  width: 26px;
  height: 1px;
  background: var(--teal);
}

#hero .nm {
  font-family: var(--D);
  font-size: clamp(3rem, 8.5vw, 6.5rem);
  font-weight: 700;
  line-height: 0.94;
  letter-spacing: -0.03em;
  margin: 0 0 0.4rem;
}

#hero .nm .d {
  color: var(--m);
  font-weight: 300;
}

#hero .creds {
  font-family: var(--D);
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  color: var(--m);
  margin: 0 0 1.5rem;
}

#hero .creds span {
  color: var(--teal);
  margin-right: 0.5rem;
}

#hero .hp {
  font-size: 0.98rem;
  color: var(--mb);
  max-width: 520px;
  line-height: 1.85;
  margin: 0 0 2.5rem;
  font-weight: 300;
}

#hero .hp strong {
  color: var(--w);
  font-weight: 500;
}

#hero .hb {
  display: flex;
  gap: 0.8rem;
  flex-wrap: wrap;
}

#hero .bp {
  font-family: var(--D);
  font-size: 0.83rem;
  font-weight: 600;
  background: var(--teal);
  color: var(--ink);
  padding: 0.72rem 1.65rem;
  border-radius: 3px;
  text-decoration: none;
  letter-spacing: 0.03em;
  transition: opacity 0.2s;
  display: inline-block;
  min-height: 44px;
}

#hero .bp:hover {
  opacity: 0.88;
}

#hero .bo {
  font-family: var(--D);
  font-size: 0.83rem;
  font-weight: 500;
  color: var(--w);
  border: 1px solid var(--bm);
  padding: 0.72rem 1.65rem;
  border-radius: 3px;
  text-decoration: none;
  letter-spacing: 0.03em;
  transition: border-color 0.2s;
  display: inline-block;
  min-height: 44px;
}

#hero .bo:hover {
  border-color: var(--mb);
}

@keyframes cw {
  to {
    transform: rotate(360deg);
  }
}

@keyframes ccw {
  to {
    transform: rotate(-360deg);
  }
}

@media (max-width: 960px) {
  #hero {
    grid-template-columns: 1fr;
    grid-template-areas:
      "content"
      "visual";
    min-height: auto;
    gap: 1.5rem;
    padding-top: 6rem;
  }

  #hero .hero__visual {
    justify-self: start;
    justify-content: flex-start;
    margin-top: 1rem;
  }

  #hero .hero__portrait {
    width: min(100%, 360px);
    margin-left: 0;
  }
}


</style>