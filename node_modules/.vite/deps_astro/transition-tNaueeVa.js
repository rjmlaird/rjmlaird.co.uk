import { markHTMLString } from "./astro_runtime_server_escape__js.js";
import { n as slide, t as fade } from "./transitions-C4CHu-_O.js";
//#region node_modules/astro/dist/transitions/cssesc.js
var regexAnySingleEscape = /[ -,\.\/:-@\[-\^`\{-~]/;
var regexSingleEscape = /[ -,\.\/:-@\[\]\^`\{-~]/;
var regexExcessiveSpaces = /(^|\\+)?(\\[A-F0-9]{1,6})\x20(?![a-fA-F0-9\x20])/g;
var DEFAULT_OPTIONS = {
	escapeEverything: false,
	isIdentifier: false,
	quotes: "single",
	wrap: false
};
function cssesc(string, options = {}) {
	options = {
		...DEFAULT_OPTIONS,
		...options
	};
	const quote = options.quotes === "double" ? "\"" : "'";
	const { isIdentifier } = options;
	const firstChar = string.charAt(0);
	let output = "";
	let counter = 0;
	const length = string.length;
	while (counter < length) {
		const character = string.charAt(counter++);
		let codePoint = character.charCodeAt(0);
		let value;
		if (codePoint < 32 || codePoint > 126) {
			if (codePoint >= 55296 && codePoint <= 56319 && counter < length) {
				const extra = string.charCodeAt(counter++);
				if ((extra & 64512) === 56320) codePoint = ((codePoint & 1023) << 10) + (extra & 1023) + 65536;
				else counter--;
			}
			value = "\\" + codePoint.toString(16).toUpperCase() + " ";
		} else if (options.escapeEverything) if (regexAnySingleEscape.test(character)) value = "\\" + character;
		else value = "\\" + codePoint.toString(16).toUpperCase() + " ";
		else if (/[\t\n\f\r\x0B]/.test(character)) value = "\\" + codePoint.toString(16).toUpperCase() + " ";
		else if (character === "\\" || !isIdentifier && (character === "\"" && quote === character || character === "'" && quote === character) || isIdentifier && regexSingleEscape.test(character)) value = "\\" + character;
		else value = character;
		output += value;
	}
	if (isIdentifier) {
		if (/^-[-\d]/.test(output)) output = "\\-" + output.slice(1);
		else if (/\d/.test(firstChar)) output = "\\3" + firstChar + " " + output.slice(1);
	}
	output = output.replace(regexExcessiveSpaces, function($0, $1, $2) {
		if ($1 && $1.length % 2) return $0;
		return ($1 || "") + $2;
	});
	if (!isIdentifier && options.wrap) return quote + output + quote;
	return output;
}
//#endregion
//#region node_modules/astro/dist/runtime/server/transition.js
var transitionNameMap = /* @__PURE__ */ new WeakMap();
function incrementTransitionNumber(result) {
	let num = 1;
	if (transitionNameMap.has(result)) num = transitionNameMap.get(result) + 1;
	transitionNameMap.set(result, num);
	return num;
}
function createTransitionScope(result, hash) {
	return `astro-${hash}-${incrementTransitionNumber(result)}`;
}
var getAnimations = (name) => {
	if (name === "fade") return fade();
	if (name === "slide") return slide();
	if (typeof name === "object") return name;
};
var addPairs = (animations, stylesheet) => {
	for (const [direction, images] of Object.entries(animations)) for (const [image, rules] of Object.entries(images)) stylesheet.addAnimationPair(direction, image, rules);
};
var reEncodeValidChars = "0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ-_".split("").reduce((v, c) => (v[c.charCodeAt(0)] = c, v), []);
var reEncodeInValidStart = "-0123456789_".split("").reduce((v, c) => (v[c.charCodeAt(0)] = c, v), []);
function reEncode(s) {
	let result = "";
	let codepoint;
	for (let i = 0; i < s.length; i += (codepoint ?? 0) > 65535 ? 2 : 1) {
		codepoint = s.codePointAt(i);
		if (codepoint !== void 0) result += codepoint < 128 ? codepoint === 95 ? "__" : reEncodeValidChars[codepoint] ?? "_" + codepoint.toString(16).padStart(2, "0") : String.fromCodePoint(codepoint);
	}
	return reEncodeInValidStart[result.codePointAt(0) ?? 0] ? "_" + result : result;
}
function renderTransition(result, hash, animationName, transitionName) {
	if (typeof (transitionName ?? "") !== "string") throw new Error(`Invalid transition name {${transitionName}}`);
	if (!animationName) animationName = "fade";
	const scope = createTransitionScope(result, hash);
	const sheet = new ViewTransitionStyleSheet(scope, transitionName ? cssesc(reEncode(transitionName), { isIdentifier: true }) : scope);
	const animations = getAnimations(animationName);
	if (animations) addPairs(animations, sheet);
	else if (animationName === "none") {
		sheet.addFallback("old", "animation: none; mix-blend-mode: normal;");
		sheet.addModern("old", "animation: none; opacity: 0; mix-blend-mode: normal;");
		sheet.addAnimationRaw("new", "animation: none; mix-blend-mode: normal;");
		sheet.addModern("group", "animation: none");
	}
	const css = sheet.toString();
	result._metadata.extraHead.push(markHTMLString(`<style>${css}</style>`));
	return scope;
}
var ViewTransitionStyleSheet = class {
	modern = [];
	fallback = [];
	scope;
	name;
	constructor(scope, name) {
		this.scope = scope;
		this.name = name;
	}
	toString() {
		const { scope, name } = this;
		const [modern, fallback] = [this.modern, this.fallback].map((rules) => rules.join(""));
		return [
			`[data-astro-transition-scope="${scope}"] { view-transition-name: ${name}; }`,
			this.layer(modern),
			fallback
		].join("");
	}
	layer(cssText) {
		return cssText ? `@layer astro { ${cssText} }` : "";
	}
	addRule(target, cssText) {
		this[target].push(cssText);
	}
	addAnimationRaw(image, animation) {
		this.addModern(image, animation);
		this.addFallback(image, animation);
	}
	addModern(image, animation) {
		const { name } = this;
		this.addRule("modern", `::view-transition-${image}(${name}) { ${animation} }`);
	}
	addFallback(image, animation) {
		const { scope } = this;
		this.addRule("fallback", `[data-astro-transition-fallback="${image}"] [data-astro-transition-scope="${scope}"],
			[data-astro-transition-fallback="${image}"][data-astro-transition-scope="${scope}"] { ${animation} }`);
	}
	addAnimationPair(direction, image, rules) {
		const { scope, name } = this;
		const animation = stringifyAnimation(rules);
		const prefix = direction === "backwards" ? `[data-astro-transition=back]` : direction === "forwards" ? "" : `[data-astro-transition=${direction}]`;
		this.addRule("modern", `${prefix}::view-transition-${image}(${name}) { ${animation} }`);
		this.addRule("fallback", `${prefix}[data-astro-transition-fallback="${image}"] [data-astro-transition-scope="${scope}"],
			${prefix}[data-astro-transition-fallback="${image}"][data-astro-transition-scope="${scope}"] { ${animation} }`);
	}
};
function addAnimationProperty(builder, prop, value) {
	let arr = builder[prop];
	if (Array.isArray(arr)) arr.push(value.toString());
	else builder[prop] = [value.toString()];
}
function animationBuilder() {
	return { toString() {
		let out = "";
		for (let k in this) {
			let value = this[k];
			if (Array.isArray(value)) out += `
	${k}: ${value.join(", ")};`;
		}
		return out;
	} };
}
function stringifyAnimation(anim) {
	if (Array.isArray(anim)) return stringifyAnimations(anim);
	else return stringifyAnimations([anim]);
}
function stringifyAnimations(anims) {
	const builder = animationBuilder();
	for (const anim of anims) {
		if (anim.duration) addAnimationProperty(builder, "animation-duration", toTimeValue(anim.duration));
		if (anim.easing) addAnimationProperty(builder, "animation-timing-function", anim.easing);
		if (anim.direction) addAnimationProperty(builder, "animation-direction", anim.direction);
		if (anim.delay) addAnimationProperty(builder, "animation-delay", anim.delay);
		if (anim.fillMode) addAnimationProperty(builder, "animation-fill-mode", anim.fillMode);
		addAnimationProperty(builder, "animation-name", anim.name);
	}
	return builder.toString();
}
function toTimeValue(num) {
	return typeof num === "number" ? num + "ms" : num;
}
//#endregion
export { stringifyAnimation as a, renderTransition as i, createTransitionScope as n, stringifyAnimations as o, reEncode as r, toTimeValue as s, ViewTransitionStyleSheet as t };
