//#region node_modules/astro/dist/runtime/client/idle.js
var idleDirective = (load, options) => {
	const cb = async () => {
		await (await load())();
	};
	const idleOptions = { timeout: (typeof options.value === "object" ? options.value : void 0)?.timeout };
	if ("requestIdleCallback" in window) window.requestIdleCallback(cb, idleOptions);
	else setTimeout(cb, idleOptions.timeout || 200);
};
var idle_default = idleDirective;
//#endregion
export { idle_default as default };
