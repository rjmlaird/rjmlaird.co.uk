//#region node_modules/astro/dist/runtime/client/visible.js
var visibleDirective = (load, options, el) => {
	const cb = async () => {
		await (await load())();
	};
	const ioOptions = { rootMargin: (typeof options.value === "object" ? options.value : void 0)?.rootMargin };
	const io = new IntersectionObserver((entries) => {
		for (const entry of entries) {
			if (!entry.isIntersecting) continue;
			io.disconnect();
			cb();
			break;
		}
	}, ioOptions);
	for (const child of el.children) io.observe(child);
};
var visible_default = visibleDirective;
//#endregion
export { visible_default as default };
