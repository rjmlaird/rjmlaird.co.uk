//#region node_modules/astro/dist/runtime/client/media.js
var mediaDirective = (load, options) => {
	const cb = async () => {
		await (await load())();
	};
	if (options.value) {
		const mql = matchMedia(options.value);
		if (mql.matches) cb();
		else mql.addEventListener("change", cb, { once: true });
	}
};
var media_default = mediaDirective;
//#endregion
export { media_default as default };
